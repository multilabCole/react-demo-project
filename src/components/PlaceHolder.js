// imports
import React from 'react'

// main call and render
const PlaceHolder = props => {
    // Hook setup

    // HTML render
    return (
        <h1>Hello wolrd</h1>
    )
}

// export
export default PlaceHolder;